### TODO:
3. collect the human prompt, model used
4. collect human terminal commands as well, input <> output
5. how to upload or make the single dataset version
   - sync to a @username, GCP bucket, versioning
   - create a PR to this repo that contains file URL + some stats about the new data added, if there's one open from this user already, update it
   - dashboard to visualize the traces
   - extra fields to define step i to j is atempting a task
 - ask for commits to start collection if git log is not empty, it shouldn't start collecting

-----

1. collect agent thinking traces and output
2. human agent should be slightly more precise
3. collect questions asked as well
4. collect codex changes, claude-code changes
5. tab completion, should store human and agents parts separately.